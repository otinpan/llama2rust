===================================================================
--- lib.rs (before)	snapshot
+++ lib.rs (after)	external
@@ -3,5 +3,4 @@
 pub mod state;
 pub mod weights;
 pub mod checkpoint;
 pub mod tokenizer;
-pub mod tokenizer;
